(library
 (name tensor_playground)
 (inline_tests)
 (preprocess
  (pps ppx_expect))
 (libraries fmt))

(menhir
 (modules Einsum_parser)
 (flags --explain --dump))

(ocamllex
 (modules Einsum_lexer))

(menhir
 (modules Type_parser))

(ocamllex
 (modules Type_lexer))
