(library
 (name tensor_playground)
 (inline_tests)
 (preprocess
  (pps ppx_expect))
 (libraries fmt menhirLib))

(menhir
 (modules Einsum_parser)
 (flags --table))

(ocamllex
 (modules Einsum_lexer))

(menhir
 (modules Type_parser)
 (flags --table))

(ocamllex
 (modules Type_lexer))
